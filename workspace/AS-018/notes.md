# Notas — AS-018

Cartão [xY300e6c](https://trello.com/c/xY300e6c). Pós AS-009.

**Já decidido:**
- UI de produto = Modules; PS **sem** UI (ADR-0005)
- M-02 Clinical Workspace = shell (ADR-0009)
- Product API para UI (ADR-0016); BFF Deferred até esta sessão
- AuthZ de verdade no servidor (ADR-0020) — UI não é perímetro de segurança
- MVP Must: M-01…M-06 + M-09; Portal M-08 = Should (ADR-0019)
- OQ-C03 aberto: UI composition contract / slot mínimo no Core

**Escopo:** superfícies de app, shell, composição, BFF opcional, Auth UX — **não** CSS, design system produto nem stack.
